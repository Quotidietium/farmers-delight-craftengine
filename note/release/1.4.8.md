# 1.4.8 — UX 轮 7：切割配方查询

## 交互优化

### /fd cutting 查询命令
- 手持任意食材输入 `/fd cutting`，列出它的全部切割配方：所需工具、产物与数量、概率（<100% 时显示百分比）。
- 空手提示用法；无配方的物品明确告知。
- 覆盖全部 106 条切割配方；空手右键砧板失败时也知道去哪查。

### /fd guide 同步
- 小刀条目补充「手持食材输入 /fd cutting 可查询切割产物」。

### /fd Tab 补全
- 补全列表更新为 `help / version / status / guide / cutting`（guide 上轮漏加，本轮一并补上）。

## 验证
- Gradle 构建：BUILD SUCCESSFUL，产物 `farmers-delight-papo-1.4.8.jar`。
- 冒烟：0 错误、backstabbing resolved（size=44 门禁）、`/fd cutting` 查询输出正常。
