# 1.4.7 — UX 轮 6：/fdplace 补全 + 取餐引导

## 交互优化

### /fdplace 管理命令体验
- Tab 补全：第 1 参提示全部可放置 id（家具 + `block:` 前缀方块）；坐标参对玩家提示脚下坐标；世界参提示世界名。
- 用法错误时列出全部可用 id；方块 id 漏写 `block:` 前缀时给出提示。

### 锅 GUI 取餐引导（UX 叠加）
- 点击餐点槽被拦截时，若餐点已就绪且容器槽为空，actionbar 提示「手持匹配的容器右键锅可取餐」。
- 两套锅 GUI（物品锅 / 方块锅）一致。

## 验证
- Gradle 构建：BUILD SUCCESSFUL，产物 `farmers-delight-papo-1.4.7.jar`。
- 冒烟：0 错误、backstabbing resolved（size=44 门禁）、`/fd version` 正常。
